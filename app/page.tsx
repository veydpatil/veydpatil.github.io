import { ArrowUpRight } from 'lucide-react';
import { projects } from './project-data';

export default function Home() {
  const categories = ['Mechanical Design', 'Robotics'] as const;
  return <main className="page"><header className="shell site-header"><a className="site-name" href="/">Veyd Patil</a><nav aria-label="Primary navigation"><a href="#mechanical-design">Mechanical Design</a><a href="#robotics">Robotics</a><a href="#contact">Contact</a></nav></header><section className="shell intro"><p className="label">Engineering portfolio</p><h1>Projects</h1><p>Mechanical design and robotics work, documented with descriptions, technical notes, and demonstrations.</p></section>{categories.map((category) => { const categoryProjects = projects.filter((project) => project.category === category); const id = category === 'Mechanical Design' ? 'mechanical-design' : 'robotics'; return <section id={id} className="shell projects project-category" aria-label={`${category} projects`} key={category}><div className="section-heading"><p className="label">{category}</p><p>{category === 'Mechanical Design' ? 'Physical design work, prototypes, and supporting media.' : 'Systems, control, planning, localization, and field robotics projects.'}</p></div>{categoryProjects.map((project, index) => <article className="project-row" key={project.slug}><p className="project-index">{String(index + 1).padStart(2, '0')}</p><div><p className="project-label">{project.label}</p><h2><a href={`/projects/${project.slug}`}>{project.title}</a></h2><p>{project.summary}</p></div><div className="project-meta"><span>{project.date}</span><a href={`/projects/${project.slug}`} aria-label={`Read about ${project.title}`}><ArrowUpRight className="size-4" /></a></div></article>)}</section>})}<footer id="contact" className="shell footer"><p>Veyd Patil</p><a href="https://github.com/veydpatil" target="_blank" rel="noreferrer">GitHub <ArrowUpRight className="inline size-3.5" /></a></footer></main>;
}
