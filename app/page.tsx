import { ArrowUpRight } from 'lucide-react';
import { projects } from './project-data';

export default function Home() {
  return <main className="page"><header className="shell site-header"><a className="site-name" href="/">Veyd Patil</a><nav aria-label="Primary navigation"><a href="#robotics">Robotics</a><a href="#contact">Contact</a></nav></header><section className="shell intro"><p className="label">Engineering portfolio</p><h1>Robotics</h1><p>Projects from independent work and CMU General Robotics, with descriptions, implementation notes, and demonstrations.</p></section><section id="robotics" className="shell projects" aria-label="Robotics projects"><div className="section-heading"><p className="label">Project archive</p><p>Click a project for its challenge statement, working approach, and available media.</p></div>{projects.map((project, index) => <article className="project-row" key={project.slug}><p className="project-index">{String(index + 1).padStart(2, '0')}</p><div><p className="project-label">{project.label}</p><h2><a href={`/projects/${project.slug}`}>{project.title}</a></h2><p>{project.summary}</p></div><div className="project-meta"><span>{project.date}</span><a href={`/projects/${project.slug}`} aria-label={`Read about ${project.title}`}><ArrowUpRight className="size-4" /></a></div></article>)}</section><footer id="contact" className="shell footer"><p>Veyd Patil</p><a href="https://github.com/veydpatil" target="_blank" rel="noreferrer">GitHub <ArrowUpRight className="inline size-3.5" /></a></footer></main>;
}
